package main

import "runtime"

// Получение JSON категорий (https://api.wallapop.com/api/v3/categories?context=search)

// Получение всех товаров в категории ()

func init() {
	runtime.GOMAXPROCS(runtime.NumCPU())
}

func main() {

}
